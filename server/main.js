'use strict';

import { Meteor } from 'meteor/meteor';

import '/imports/startup/server';

Meteor.startup(() => {
	//TODO: do server startup things here, such as modifying onConnection, onCreateUser and/or validateLoginAttempt
});
